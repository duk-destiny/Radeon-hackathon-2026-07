# Spec: 阶段 G 复核修复

| 字段 | 值 |
|---|---|
| Spec ID | `stage-g-review-fix-260722-2300` |
| 变更级别 | S2 |
| 日期 | 2026-07-22 |
| 状态 | implemented / verified |

## 背景

阶段 G 风险扫描器最初只读取旧版 `tasks` 表，而阶段 F 的正式任务生命周期使用 `task` 表。真实项目会因此被扫描为零任务。文档版本检测还需要确保调用方能按项目范围读取版本记录，并拒绝越出项目根目录的文件路径。

## 验收

- [x] 风险扫描优先读取阶段 F 的 `task` 表，并保留对旧 `tasks` 表的兼容。
- [x] 变更影响分析同时支持正式表和旧表。
- [x] 文档版本检测支持按 `project_id` 过滤当前版本。
- [x] 文档版本检测拒绝 `../` 等越界路径。
- [x] 阶段 F 表中的逾期任务可被阶段 G 扫描并生成风险记录。
