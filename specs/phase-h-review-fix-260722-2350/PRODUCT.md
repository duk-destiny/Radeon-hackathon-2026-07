# Spec: 阶段 H 复核修复

| 字段 | 值 |
|---|---|
| Spec ID | `phase-h-review-fix-260722-2350` |
| 变更级别 | S2 |
| 日期 | 2026-07-22 |
| 状态 | implemented / verified |

## 背景

阶段 H 的协作看板、总览和风险中心必须展示 F/G 已持久化的真实项目数据。复核发现其读取了项目目录下的另一套数据库路径，而正式任务与风险数据位于项目 SQLite 任务库。此外，被禁用账号在 Token 过期前仍可访问系统。

## 验收

- [x] 看板读取阶段 F 任务库并展示已创建任务。
- [x] 总览与风险中心使用与阶段 F/G 一致的项目 SQLite 数据库。
- [x] 风险中心首次访问时可初始化阶段 G 风险表。
- [x] 被禁用用户的既有 Bearer Token 返回 401。
